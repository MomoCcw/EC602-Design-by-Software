#include "ec602.hpp"
using namespace ec602;

int main(int argc, char** argv) {
  if (argc != 4) return 1;
  string s;
  int from,to;

  s = argv[1];
  from = stoi(argv[2]);
  to = stoi(argv[3]);
      
}